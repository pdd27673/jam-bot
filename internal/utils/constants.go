package utils

// command represents a bot comamnd=
type Command string

const (
	COMMAND_PING Command = "ping"
	COMMAND_HELP Command = "help"
)
const (
	DISCORD_BOT_PREFIX = "!" // the prefix for all bot commands
)
